/**
 * Resource
 */

var ƒ = require('effd'),
    debug = require('debug')('resources:resource'),
    debugˆ = ƒ.passthrough(debug);

module.exports = function() {
    
    /* Function format
    var func = input =>
        debugˆ('func %s', input)
        .then(input => input)
        .catch();
    */
    
    var interface = {
        
    };
    
    return ƒ(interface);
};